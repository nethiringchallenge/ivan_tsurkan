using System;
using Chatb_bot.Strategy;

namespace Chatb_bot
{
       public class CalculateCommand : Command
    {
        public CalculateCommand(string param)
        {
        }

        public override void Execute()
        {
            throw new NotImplementedException();
        }
    }
}